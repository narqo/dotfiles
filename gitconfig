[user]
    name = Vladimir Varankin
    email = nek.narqo+git@gmail.com
[core]
    editor = /usr/bin/vim
    excludesfile = ~/.gitignore
    whitespace = fix,-indent-with-non-tab,trailing-space,-cr-at-eol
[diff]
    algorithm = patience
[github]
    user = narqo
[alias]
    di = diff
    ci = commit
    co = checkout
    st = status
    hist = log --pretty=format:\"%h %ad | %s%Cgreen%d%Creset [%an]\" --graph --date=short
    ls = ls-files
    ff = merge --ff-only
    # list files marked with "--assume-unchanged"
    mu = !git ls-files -v | grep "^[[:lower:]]"
[color]
    ui = auto
    branch = auto
    diff = auto
    interactive = auto
    status = auto
