[user]
    name = Vladimir Varankin
    email = nek.narqo+git@gmail.com
[github]
    user = narqo
[core]
    editor = vim
    excludesfile = ~/.gitignore
    whitespace = fix,-indent-with-non-tab,trailing-space,-cr-at-eol
[diff]
    algorithm = patience
[push]
    default = simple
[alias]
    br = branch -v
    ci = commit
    co = checkout
    di = diff
    hist = log --graph --pretty=format:\"%C(bold red)%h%Creset %ad - %s%C(green)%d%Creset %C(blue)<%an>%Creset\" --date=short
    ff = merge --ff-only
    ls = ls-files
    # list files marked with "--assume-unchanged"
    mu = !git ls-files -v | grep "^[[:lower:]]"
    st = status
[color]
    ui = auto
    branch = auto
    diff = auto
    interactive = auto
    status = auto
